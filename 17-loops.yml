- name: install packages
  hosts: local
  connection: local
  become: true
  tasks:
    - name: install packages using loop
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - git
        - vim
        - wget