- name: demon on filters
  hosts: local
  connection: local
  vars:
    topics: linux ,shell ,ansible ,terraform
  tasks:
    - name: filter test
      ansible.builtin.debug:
        msg: "Hi Chanakya , {{ greeting | default('Good Morning') }} "

    - name: convert string to list
      ansible.builtin.debug:
        msg: "{{ topics | split(',') }}"
      